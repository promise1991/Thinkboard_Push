server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  # Optional: forward API calls to backend
  location /api/ {
    proxy_pass http://backend:5000;
  }
}